using System;
using DeviceSolutions.SPOT.Hardware;
using Microsoft.SPOT;
using Microsoft.SPOT.Hardware;

namespace BitBangTest
{
    public class Program
    {
        public static void Main()
        {
            //for (int i = 0; i < boolBufferLength; i++)
            //{
            //    Debug.Print(
            //        (boolBuffer[i].v[0] ? "1" : "0") +
            //        (boolBuffer[i].v[1] ? "1" : "0") +
            //        (boolBuffer[i].v[2] ? "1" : "0") +
            //        (boolBuffer[i].v[3] ? "1" : "0") +
            //        (boolBuffer[i].v[4] ? "1" : "0") +
            //        (boolBuffer[i].v[5] ? "1" : "0") +
            //        (boolBuffer[i].v[6] ? "1" : "0") +
            //        (boolBuffer[i].v[7] ? "1" : "0"));
            //}

        }
    }
}
